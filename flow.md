# 배드민턴 모임 관리 서비스 개발 현황 및 향후 계획

## 1. 현재까지 완성된 기능
현재 서비스는 배드민턴 모임의 핵심적인 운영과 사용자 관리를 위한 기본적인 기능들을 견고하게 갖추고 있습니다.

### 1.1. 인증 및 계정 시스템
*   **로그인/로그아웃**: Zustand를 활용하여 사용자 인증 상태를 전역적으로 관리합니다.
*   **세션 유지**: `persist` 미들웨어를 통해 새로고침 후에도 로그인 상태가 유지됩니다.
*   **회원가입 및 중복 체크**: 사용자 ID 중복 확인 로직이 포함된 가입 기능을 제공합니다.
*   **자동 퇴장 처리**: 로그아웃 시 사용자가 '입장' 상태라면 서버에 퇴장 요청을 보내 데이터 무결성을 보장합니다.

### 1.2. 실시간 입장/퇴장 시스템
*   **입장 (Entry)**: 참여 시 `isPresent`를 `true`로, 상태를 '휴식중'으로 변경하여 명단에 노출합니다.
*   **퇴장 (Exit)**: 참여 종료 시 상태를 업데이트하며, 경기 중일 경우 해당 경기를 무효화하는 안전장치가 작동합니다.
*   **상태 동기화**: React Query와 Zustand를 연동하여 내 정보와 전체 명단이 실시간으로 동기화됩니다.

### 1.3. 유저 상태 및 명단 관리
*   **상태별 자동 분류**: 유저를 '경기중', '대기중', '휴식중' 섹션으로 자동 분류합니다.
*   **매칭 선호도 그룹화**: 대기 중인 유저를 선호 방식(남복, 여복, 혼복 등)에 따라 카테고리화합니다.
*   **자동 최신화**: 서버 데이터 변경 시 캐시 무효화(Invalidation)를 통해 명단을 실시간으로 유지합니다.

### 1.4. 경기 및 매칭 로직
*   **매칭 권한 제어**: 실제로 입장한 유저에게만 매칭 생성/참여 권한을 부여합니다.
*   **결과 반영 및 Elo 반영**: 경기 종료 시 승패 결과에 따라 유저의 Elo 레이팅을 자동으로 계산하고 업데이트합니다.

---

## 2. 앞으로 개발해야 할 기능 (향후 계획)

### 2.1. 데이터 시각화 및 통계
*   **전적 상세 분석 (Record)**:
    *   **최근 전적**: `me.id` 기반 필터링 및 최근 10경기(Default) 승률/승패 흐름 시각화.
    *   **소셜 통계**: 자주 플레이한 인원 랭킹 및 빈도 계산.
    *   **베스트 파트너**: 파트너별 동반 승률 분석을 통한 최적의 조합 도출.
    *   **천적(Nemesis) 시스템**: 상대 전적이 낮은 유저를 분석하여 실력 향상 동기 부여.
*   **레이팅 추이 그래프**: 내 실력 변화를 한눈에 볼 수 있는 차트 도입.

### 2.4. 소셜 기능 강화
*   **실시간 알림**: 매칭 완료 알림 및 대기 유저 호출 기능.

### 2.5. 경기 기록 상세 보기 (스코어/레이팅 제외)
*   **내용**: `History` 페이지에서 각 경기 기록을 클릭하면, 해당 경기에 참여했던 **유저들의 이름(UserCard 형태)**을 보여주는 상세 모달 또는 확장 가능한 UI를 추가합니다.
*   **기대 효과**: 과거 경기의 참여자들을 쉽게 확인하여 기억을 되짚거나, 특정 유저와의 매칭 이력을 파악하는 데 도움을 줍니다.

### 2.6. 대기 유저 호출 기능
*   **내용**: 관리자가 `WAITING` 상태의 특정 유저를 선택하여 "코트로 와주세요"와 같은 실시간 알림을 보낼 수 있는 기능을 추가합니다.
*   **기대 효과**: 매칭이 잡혔을 때 유저가 자리에 없는 경우, 관리자가 직접 찾아다니는 수고를 덜고 효율적인 경기 진행을 돕습니다.

### 2.7. UserCard 디자인 개선 (성별/상태 시각적 명확성)
*   **내용**: `UserCard.jsx`에서 성별에 따른 배경색과 상태에 따른 테두리 색상이 겹쳐 보일 수 있는 부분을 개선합니다. 예를 들어, `PLAYING` 상태일 때 테두리를 더 두껍게 하거나, 유저 이름 옆에 작은 상태 아이콘/배지를 추가하여 성별과 상태를 동시에 명확하게 인지할 수 있도록 합니다.
*   **기대 효과**: 명단에서 유저들의 상태를 더욱 직관적으로 파악할 수 있어 UI/UX를 향상시킵니다.